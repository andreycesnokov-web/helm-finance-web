# Summaries — compliance

> Every source below is `status: collected`. Nothing here is verified law. Rates quoted are candidates awaiting a licensed reviewer — see `../../reviewer_notes.md`.

Orientation notes written from official sources, for a reviewer to read before the primary text. **A summary is never a citation.** When ingesting for RAG, embed the primary document from `raw_sources/`, not these files.

See the per-topic README under `../../compliance/` for the source list and gaps.
